<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Weather Entry</title>
  <link rel="stylesheet" href="/stylesheet/style.css">
</head>
<body>
  <%- include('../partials/_navbar') %>
  <main>
    <h1>Edit Weather Entry</h1>
    <form action="/weather/" method="POST">
      <div>
        <label for="type">Type:</label>
        <input type="text" id="type" name="type" value="<%= entry.type %>" required>
      </div>
      <div>
        <label for="notes">Notes:</label>
        <textarea id="notes" name="notes" rows="4" cols="40"><%= entry.notes %></textarea>
      </div>
      <div>
        <label for="fahrenheit">Fahrenheit:</label>
        <input type="number" id="fahrenheit" name="fahrenheit" value="<%= entry.fahrenheit %>" required>
      </div>
      <div>
        <label for="state">State:</label>
        <input type="text" id="state" name="state" value="<%= entry.state %>" required>
      </div>
      <button type="submit">Save Changes</button>
    </form>
    <p><a href="/weather">: Back to Add a new Tracker</a></p>
    <form
  action="/weather"
  method="POST"
></form>
  </main>
</body>
</html>
